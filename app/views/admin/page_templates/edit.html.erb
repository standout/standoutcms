<div>
  <h2 style="display: inline-block">
    <%= @page_template.name %>
  </h2>
  <% unless @page_template.used? %>
    <%= link_to t("delete"), [:admin, @page_template.look, @page_template],
      method: "delete", data: { confirm: t("really_delete") } %>
  <% end %>
</div>
<%= render :partial => 'form' %>

<% javascript "page_templates" %>
<% stylesheet "page_templates" %>
