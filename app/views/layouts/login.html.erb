<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>Standout CMS: Publiceringsverktyg</title>
  <%= stylesheet_link_tag 'scaffold', 'websites_grid', 'login' %>
  <%= javascript_include_tag 'jquery', 'users' %>
</head>
<body>

<% [:notice, :alert].each do |key| %>
    <% if flash[key] %>
    <div class="<%= key %>" id="flash">
      <%= flash[key] %>
    </div>
    <% end %>
<% end %>

<div id="wrapper">
  <div id="menu">
    <%= link_to "Log out", :controller => 'users', :action => 'logout' if @me %>
    <%= link_to "Users", users_path if is_admin? %>
  </div>

  <%= yield  %>
</div>

</body>
</html>
