Hej!

Jag såg att du behövde ett nytt lösenord, så jag skickar över en länk där du själv kan välja
ett nytt lösenord direkt.

<%= url_for(:controller => 'users', :action => 'password_reset', :id => @user.id, :code => @user.password_reset_code, :only_path => false) %>

Länken för att nollställa lösenordet kommer bara att fungera en gång, så du kan
slänga det här mailet när du är klar.

/ David

----------------------------------
Standout AB
http://standout.se/ - 0470-348700